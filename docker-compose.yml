# 컨테이너 로그 상한 — json-file 기본값은 무제한이라 장시간 가동 시
# (특히 요청마다 access log를 남기는 app과 폴링을 받는 mocktelegram)
# 디스크가 로그로 잠식된다. 같은 계열의 무제한 증가를 원천 차단.
x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"

services:
  postgres:
    image: postgres:16-alpine
    logging: *default-logging
    environment:
      POSTGRES_USER: telegram
      POSTGRES_PASSWORD: telegram
      POSTGRES_DB: telegram_server
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U telegram -d telegram_server"]
      interval: 2s
      timeout: 2s
      retries: 20
    ports:
      - "127.0.0.1:5433:5432"

  migrate:
    image: migrate/migrate:v4.18.1
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./migrations:/migrations
    command: [
      "-path", "/migrations",
      "-database", "postgres://telegram:telegram@postgres:5432/telegram_server?sslmode=disable",
      "up"
    ]
    restart: "no"

  mocktelegram:
    build:
      context: .
      dockerfile: Dockerfile.mocktelegram
    logging: *default-logging
    environment:
      MOCKTELEGRAM_ADDR: ":8090"
    ports:
      - "127.0.0.1:8090:8090"

  app:
    build: .
    logging: *default-logging
    depends_on:
      migrate:
        condition: service_completed_successfully
      mocktelegram:
        condition: service_started
    environment:
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:-1:AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA}
      TELEGRAM_BOT_USERNAME: ${TELEGRAM_BOT_USERNAME:-DJ_notification_bot}
      # Dev/test default: redirect telego to mocktelegram. Unset / override
      # via .env for staging or production where it must hit api.telegram.org.
      TELEGRAM_API_URL: ${TELEGRAM_API_URL:-http://mocktelegram:8090}
      DATABASE_URL: postgres://telegram:telegram@postgres:5432/telegram_server?sslmode=disable
      HTTP_LISTEN_ADDR: "0.0.0.0:8080"
      LOG_LEVEL: info
    ports:
      - "127.0.0.1:8080:8080"

  adminui:
    build:
      context: .
      dockerfile: Dockerfile.adminui
    logging: *default-logging
    depends_on:
      migrate:
        condition: service_completed_successfully
      app:
        condition: service_started
    environment:
      # Placeholder password, same convention as TELEGRAM_BOT_TOKEN above —
      # operators must override via .env before exposing beyond loopback.
      ADMINUI_PASSWORD: ${ADMINUI_PASSWORD:-change-me-admin-pw}
      # Dev seed admin key (cmd/devseed). Override via .env in staging/prod.
      ADMINUI_API_KEY: ${ADMINUI_API_KEY:-tg_devadmin_0123456789abcdef0123456789abcdef}
      ADMINUI_LISTEN_ADDR: "0.0.0.0:8081"
      TELEGRAM_SERVER_URL: http://app:8080
      DATABASE_URL: postgres://telegram:telegram@postgres:5432/telegram_server?sslmode=disable
    ports:
      - "127.0.0.1:8081:8081"

volumes:
  pgdata: {}
